import { ContentList } from '../../components/ContentList';

export const Content = () => {
    return (
        <div>
            <h1>Content Page!!</h1>
            <ContentList />
        </div>
    );
}